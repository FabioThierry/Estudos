// Crie uma função que receba dois parâmetros (um número e uma string) e retorne uma mensagem concatenando os dois valores.

let doisParametrosConcatenados = (string, numero) => {
  return `${string} ${numero}`;
};

console.log(doisParametrosConcatenados("Quantidade de queijos vendidos:", 14));
